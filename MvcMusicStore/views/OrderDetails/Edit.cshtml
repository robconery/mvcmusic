@model MvcMusicStore.Models.OrderDetail

@{
    ViewBag.Title = "Edit";
    Layout = "~/views/Shared/_Layout.cshtml";
}
<div class = "container">
    <div class = "row span12">
		<h2>Edit</h2>

								<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
		<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

				@using (Html.BeginForm()) {
			@Html.ValidationSummary(true)
			<fieldset>
				<legend>OrderDetail</legend>

						@Html.HiddenFor(model => model.OrderDetailId)

						 @Forms.TextBox("OrderId",@Model.OrderId)

						 @Forms.TextBox("Sku",@Model.Sku)

						 @Forms.TextBox("UnitPrice",@Model.UnitPrice)

						 @Forms.TextBox("Quantity",@Model.Quantity)

						 @Forms.TextBox("Name",@Model.Name)

						 @Forms.TextBox("Discount",@Model.Discount)

						<p>
					<input class="btn btn-primary" type="submit" value="Save" />
				</p>
			</fieldset>
		}

		<div>
			@Html.ActionLink("Back to List", "Index")
		</div>

	</div>
</div>

